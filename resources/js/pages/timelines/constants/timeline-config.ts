import { BreadcrumbItem } from '@/types';

export const TIMELINE_BREADCRUMBS: BreadcrumbItem[] = [
    {
        title: 'Timelines',
        href: '#',
    },
];

export const TIMELINE_TOTAL_WEEKS = 12;

export const TIMELINE_INITIAL_DATE = new Date(2026, 0, 1);
